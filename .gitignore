
# Model files
models/
*.gguf
*.safetensors
/vexel
/inference/vexel
vexel_metal
vexel_old
.gocache/

# Benchmark environment
benchmarks/.venv/
benchmarks/models/
benchmarks/results/*
# ...but keep the result SCHEMA + canonical example (source artifacts, not run output).
# Note: ignore the dir CONTENTS (/*) not the dir itself, so these negations apply.
!benchmarks/results/SCHEMA.md
!benchmarks/results/example_result.json

# Python bytecode (benchmarks/ helper scripts)
__pycache__/
*.pyc

# Memory/cache
.remember/

# Debug tools and test data
compare_layers
data/
tools/
